<div class="padded">




  <%= form_tag products_search_path do %>

  <div class="container">
    <div class="row">
    <h3>Sélectionnez un ou plusieurs looks :</h3>
    <%Look.all.each do |look| %>
      <div class="col-xs-12 col-sm-4 col-md-3">
        <div class="card-look" >
            <div class="card-name"><%= look.name %> </div>
            <div class="card-filter" style="background-image: url('<%= image_path look.photo %>') ">
            </div>
            <div class="card-check hide">
              <i class="fa fa-check"></i>
            </div>
            <input type="hidden" name="looks[<%= look.id.to_s %>]" value="0">
        </div>
      </div>
      <%end %>
    </div>
  </div>

  <div class="text-center">
    <input type="submit" value="Rechercher" class="btn btn-primary">
  </div>

  <% end %>


</div>


<% content_for :after_js do %>

<script>
  $(function(){
    $('.card-look').click(function() {
      $(this).children(".card-check").toggleClass('hide');
      var el = $(this).children('input[type=hidden]');
      el.val(el.val() == 0 ? 1 : 0);
    });
  });
</script>

<% end %>
