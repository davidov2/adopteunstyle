

<div class=sidebar>
  sidebar
</div>







<div class="wrapper-search">

  <h3 class="text-center"> Produits disponibles auprés de nos partenaires correspondant à votre choix :</h3>

  <div class="container">
    <div class="row">
    <div class="card-product">
     <% @products.each do |p| %>
            <%= p.title %>
            <%= p.description %>
            <%= p.size %>
            <%= p.price %>
            <%= p.color %>
            <%= p.brand %>
            <%= p.category %>
        <% end %>
    </div>

   </div>
  </div>

  <div class="container padded-basket text-center">
    <%= link_to "Retour aux styles", root_path, :class => "btn btn-danger" %>
  </div>


  </div>
