


<div class="padded">
            <%= link_to " Retour", products_path, :class => "text-right" %>

  <div class="container">
    <div class="row">
      <div class="col-xs-12 col-md-6">
      <div class="border-gray">
          <%= image_tag @product.image %>
        </div>
      </div>

          <div class="col-xs-12 col-md-6">
            <h4><%= @product.title %></h4>
            <p><%= @product.description %></p>

            <p>Prix : <%= @product.price %>€</p>

            <p>Tailles disponibles :</p>

            <ul class="list-inline">
              <% @product.size_array.each do |size| %>
                <li class="card-size">
                  <%= size %>
                </li>
              <%end%>

            </ul>


            <%= link_to "Acheter", @product.link, :class => "btn btn-danger" %>

          </div>

        </div>
      </div>
    </div>

